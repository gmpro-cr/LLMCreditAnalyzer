
## Harness Rules

See global `~/.claude/CLAUDE.md` for the universal gate (Plan → Build → Verify → Commit → Push).

**Project-specific:**
- No Supabase schema changes without a migration file in /migrations

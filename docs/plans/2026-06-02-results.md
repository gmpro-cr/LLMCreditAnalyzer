# Credit Integrity & Tabulated CAM — Results (before / after)

Implemented from `2026-05-31-credit-integrity-and-cam-formatting.md`. ₹0 stack hardening.
All changes test-driven (32 passing tests) and verified live on Tata Steel + Reliance.

## Number correctness

| Field | Before (buggy) | After | Root cause fixed |
|---|---|---|---|
| PAT (Net Profit) | `"Dividend Yield\s+..."` regex string | real 3-yr series (TISCO 15,662/13,970/16,065) | loop var `pat` shadowed the array (`public_data.py:270`) |
| TOL/TNW | 2.01 | 1.01 | numerator wrongly included net worth (`ratios.py`) |
| DSCR | 1.69 (undisclosed) | 1.12, basis=`PAT+Dep+Int`, `est.` flagged | looser OCF numerator + guessed principal, now disclosed |
| Free Cash Flow | ~OCF (31,966) | 6,298 (OCF − capex) | Screener "FCF" row misparsed to ≈OCF |
| Confidence | "HIGH" with PAT missing | clamped to MEDIUM + limitations section | no validation gate |

## Format / tables
- **Before:** prose only, numbers inline as "₹139,720.00 Cr"; no spread, no ratio table.
- **After:** deterministic **CMA spread** (years as columns, Indian lakh-crore grouping) + **ratio-vs-covenant** table with pass/breach, injected into both the 8-section memo and the 12-section case editor. DSCR now correctly shows **✗ Breach** vs the 1.25 covenant. Paise stripped from crores; basis (standalone/consolidated) disclosed; per-table source note.

## UI defects
- Dashboard "View case" links: 20× `/cases/undefined` → **0** (real IDs).
- Drawing Power calc: **hidden for Term Loans** (Tandon Method II is WC-only); ₹→Cr fixed (Sanctioned Limit `200000` → `₹2 Cr`).

## Verification
- 32 unit tests (`pytest`) green.
- Live `/public-data/generate-memo` (Tata Steel): PAT real, TOL/TNW 1.01, FCF 6,298, confidence clamped.
- Live 12-section generate (Reliance, via api-server with empty ratios → auto-computed): spread + ratio tables render in the case editor (`cg-after-financial-tables.png`).

## Known residuals (next, not in this ₹0 pass)
- Qualitative research still thin (free DuckDuckGo returns little) — needs a paid search/ratings source (your deferred budget call).
- Screener scrape remains the fragility ceiling; durable fix = licensed data API.
- `current_ratio` often not computable from Screener (flagged by the gate, caps confidence to MEDIUM).
